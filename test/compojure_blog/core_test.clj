(ns compojure-blog.core-test
  (:require [clojure.test :refer :all]
            [compojure-blog.core :refer :all]))

(deftest a-test
  (testing "Fixed failing example test. I should pass now."
    (is (= 1 1))))
